PROFILE PHOTOS — drop your files here with these exact names:
  home-portrait.jpg        → shown on the HOME page (tall 4/5 portrait)
  about-portrait.jpg       → shown large on the ABOUT page (wide 3/2)
  about-secondary.jpg      → shown offset on the ABOUT page (tall 4/5)
JPG, PNG, WebP or AVIF all work. If you use a different extension, update
the matching src in src/content/profile.ts → photos.
Use high-resolution images; the site crops and optimizes them responsively.
Until a file exists, a tasteful placeholder is shown (never a broken image).

